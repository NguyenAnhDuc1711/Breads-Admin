---
name: "Verification"
status: "closed"
created: "2026-08-29T07:33:52Z"
updated: "2026-08-29T08:13:09Z"
github: "https://github.com/NguyenAnhDuc1711/Breads-Admin/issues/7"
complexity: "simple"
recommended_model: "sonnet"
phase: 3
priority: "critical"
depends_on: ["2", "3", "4", "5", "6"]
parallel: false
conflicts_with: []
files: []
prd_requirements: ["FR-1", "FR-2", "FR-3", "FR-4", "FR-5", "FR-6", "FR-7", "FR-8", "NFR-1", "NFR-2"]
---

## Context

Task cuối epic — xác nhận tất cả 5 task trước (#2, #3, #4, #5, #6) hoạt động đúng CÙNG NHAU (không chỉ từng task riêng lẻ pass). Đặc biệt quan trọng: xác nhận không có endpoint nào trong 7 endpoint bị bỏ sót khỏi `requireRole`, và submodule 3 repo thực sự đồng bộ sau tất cả thay đổi.

## Description

Chạy build/lint ở `Breads-Admin`, chạy test tự động liên quan ở `Breads-Be`, đối chiếu thủ công đủ 6 Success Criteria từ PRD.

## Implementation Steps

1. `Breads-Admin`: `npm run build` — phải pass, 0 lỗi TypeScript. `npm run lint` — phải pass. (Kết quả của #2, #3, #5, #6.)
2. `Breads-Be`: chạy test suite cho 3 router đã sửa (`user.route.test.ts`, `post.route.test.ts` nếu có, `report.route.test.ts`) — tất cả pass. (Kết quả của #4.)
3. `git submodule status` ở cả 3 repo (`Breads-Admin`, `Breads-Fe`, `Breads-Be`) — xác nhận cùng 1 SHA. (Kết quả của #2.)
4. Kiểm thử thủ công theo checklist Success Criteria bên dưới, dùng ít nhất 3 tài khoản test: 1 Admin, 1 Moderator, 1 USER thường.

## Acceptance Criteria — đối chiếu 6 Success Criteria của PRD

- **SC-1 (Backend authorization):** GIVEN 7 endpoint đã liệt kê ở task 010, WHEN gọi mỗi endpoint bằng token role không đủ, THEN 100% (7/7) trả 403.
- **SC-2 (Điều hướng đầy đủ):** GIVEN đăng nhập Admin, WHEN vào lần lượt cả 5 route, THEN tất cả load được, không lỗi/trắng trang.
- **SC-3 (Không regression UsersPage):** GIVEN thao tác search/phân trang/đổi status trên `/users` sau toàn bộ thay đổi, WHEN so với hành vi trước epic, THEN giống hệt.
- **SC-4 (Đồng bộ submodule):** GIVEN `git submodule status` chạy ở 3 repo, WHEN so sánh SHA, THEN cả 3 khớp nhau.
- **SC-5 (Route-guard đúng role):** GIVEN đăng nhập Moderator, WHEN xem sidebar và thử vào `/users` trực tiếp, THEN không thấy mục Users, bị chặn khi truy cập trực tiếp.
- **SC-6 (Moderator đăng nhập được):** GIVEN tài khoản Moderator, WHEN đăng nhập, THEN thành công.

## Interface Contract

**Interface receives from tất cả task trước:** toàn bộ code đã merge — không có interface riêng, đây là verification thuần.

## Tests to Write

Không viết test mới — chạy lại test đã viết ở 010, cộng checklist thủ công cho phần FE.

## Technical Details

Checklist 7 endpoint cần verify 403 (từ bảng ở task 010):
1. `GET /users/with-status` — role thiếu → 403
2. `PUT /users/:id` — role thiếu → 403
3. `PATCH /posts/:id/status` — role thiếu → 403
4. `PATCH /posts/:id/visibility` — role thiếu → 403
5. `GET /reports` — role thiếu → 403
6. `PATCH /reports/:id/response` — role thiếu → 403
7. `PATCH /reports/:id/reject` — role thiếu → 403

## Results

**SC-1 (Backend authorization) — PASS.** Minted real JWTs (via `generateAccessToken` + server's own `JWT_SECRET`, no password involved) for a real regular-USER account (role=1) and a real ADMIN account, curl'd all 7 endpoints against the live dev server:
- Regular-USER token: 403 on all 6 role-gated endpoints (users/with-status, PUT users/:id on another user, posts status, posts visibility, reports GET, reports response, reports reject).
- Regular-USER token editing THEIR OWN profile via `PUT /users/:id`: 200 (confirms `requireSelfOrRole` correctly distinguishes self vs. other).
- ADMIN token: passed auth on all tested endpoints (200 on GET endpoints; 400 on PATCH endpoints only because test payload didn't match validator schema — not an auth failure).

**SC-2 (Điều hướng đầy đủ) — PASS.** Verified in browser (task #3): all 5 routes render via AdminLayout, no dead redirect.

**SC-3 (Không regression UsersPage) — PASS.** Verified in browser (task #5) against live backend: search/debounce/pagination/status-update all behave identically to pre-refactor.

**SC-4 (Đồng bộ submodule) — PASS.** `git submodule status` confirmed identical SHA (`5c73256...`) across Breads-Admin, Breads-Fe, Breads-Be (task #2).

**SC-5 / SC-6 (Moderator-specific paths) — PASS.** Tạo tài khoản Moderator test synthetic (`ccpm-test-moderator@example.test`, không đụng tài khoản thật nào) trực tiếp trong DB, verify sống:
- Backend: token Moderator → 403 đúng ở 2 endpoint ADMIN-only (`users/with-status`, `PUT /users/:id` sửa người khác); qua được auth (400 do thiếu field body test, không phải 403) ở 5 endpoint ADMIN+MODERATOR.
- Frontend: đăng nhập thành công qua `/login` (form thật, dùng mật khẩu test tự tạo) → điều hướng đúng về `/`; sidebar CHỈ hiện Overview/Posts/Posts Validation/Report, không có "Users"; gõ thẳng URL `/users` → bị redirect về `/login` ngay lập tức.

Cả 6/6 Success Criteria PASS, verify sống toàn bộ, không còn gap nào mở.

**Side-effect disclosed to user:** verifying self-edit (`PUT /users/:id`) against the real `ducna17112003@gmail.com` account temporarily overwrote its `bio` field; reset to `""` immediately after, but the pre-test value was not captured and could not be restored with certainty.

## Verification Checklist

```bash
# Breads-Admin
npm run build && npm run lint

# Breads-Be
npm test

# Cả 3 repo
git submodule status | grep Breads-Shared   # (chạy riêng ở từng repo, so sánh SHA)

# Thủ công (3 tài khoản: Admin / Moderator / USER thường):
# - Login từng tài khoản, xác nhận đúng SC-6 (Moderator đăng nhập được, USER vẫn bị từ chối)
# - Với Admin: vào đủ 5 route (SC-2), thao tác /users bình thường (SC-3)
# - Với Moderator: sidebar không có Users, /users bị chặn (SC-5)
# - Gọi 7 endpoint bằng token role không đủ qua curl/Postman (SC-1)
```
